// Shugu Forge — Design Studio shell (canvas-first, no sub-tabs).
// Inspiration catalogue / Projets / Marque tabs removed — everything lives
// on the infinite canvas (see StudioWorkspace).

import { StudioWorkspace } from "./StudioWorkspace";

export function StudioShell() {
  return <StudioWorkspace />;
}
