// Lazy route module for /studio — canvas-first Design Studio workspace.
import { StudioShell } from "@/features/studio/StudioShell";

export default function StudioShellRoute() {
  return <StudioShell />;
}
